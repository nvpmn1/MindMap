// Abra o console do navegador (F12) e cole este código para limpar e testar

// Limpar localStorage
localStorage.removeItem('skipDatabaseSetup');
localStorage.removeItem('databaseReady');

// Recarregar página
window.location.reload();
